import { LaunchNameMapperPipe } from './launch-name-mapper.pipe';

describe('LaunchNameMapperPipe', () => {
  it('create an instance', () => {
    const pipe = new LaunchNameMapperPipe();
    expect(pipe).toBeTruthy();
  });
});
